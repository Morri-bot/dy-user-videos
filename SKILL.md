---
name: dy-user-videos
description: 抖音用户作品查询工具。输入抖音视频链接、用户主页链接或sec_user_id，查看该作者全部公开发布的作品列表，支持翻页浏览、查看单条视频详情、导出Excel。当用户发送抖音用户链接（douyin.com/user/）或抖音视频链接想查作者作品，或提到「查作者」「这个作者」「作品列表」「作者视频」时使用。作者：阿南 rockmoons（抖音）微信：rockmoons API：www.rockmoons.com
---

# dy-user-videos · 抖音用户作品查询

> 作者：阿南 rockmoons（抖音）· 微信：rockmoons · API：www.rockmoons.com

## 中文编码铁律

凡处理中文的 shell 命令，一律写成 Python 脚本文件再执行，禁止在命令行直接拼接中文字符串。Python 脚本头部加 `# -*- coding: utf-8 -*-`。

---

输入一个链接，返回该作者全部作品，翻页浏览，一键导出 Excel。

---

## Step 0 · 读取配置

```
Read config.json
```

- **config.json 不存在** → 复制 `config.example.json` → 提示：
  ```
  👋 我能帮你查抖音作者的全部作品～
  只需给我链接，🔑 到 www.rockmoons.com 拿 APIKey
  ```
- **apikey 为空** → 同上引导
- **apikey 已有值** → 继续

---

## Step 1 · 解析输入 + 内部黑盒拿数据

三种输入统一处理。用户只看到「🔍 正在查询...」→ 结果。

| 用户给什么 | 内部做什么 |
|-----------|-----------|
| 抖音视频链接 | 提取 sec_user_id → API 03（列表）→ 取首条 ID → API 01（补作者数据） |
| 用户主页链接 | 提取 sec_user_id → API 03 → 取首条 ID → API 01 |
| sec_user_id | API 03 → 取首条 ID → API 01 |

**帮助入口：** 「帮助」「怎么用」→
```
📊 发抖音链接 → 查看作者全部作品
📄 下一页/上一页 → 翻页浏览
🔍 查第N个 → 看某条视频详情
📥 导出 → 一键导出 Excel
💬 示例：https://www.douyin.com/video/xxx
```

**自然语言无链接：** 「看看这个作者」→ 「我需要链接。抖音→作者主页→分享→复制链接给我～」

**非抖音链接：** 「这个链接我识别不了，请发抖音视频或用户主页链接～」

---

## Step 2 · 调用 API

### API 03（用户作品列表）

```bash
# 首页
curl -s --connect-timeout 10 --max-time 30 "{base_url}/dy_fetch_user_post?sec_user_id={id}&max_cursor=0&count=20&apikey={apikey}"

# 翻页（cursor = 上页最后一条的 dy_max_cursor）
curl -s --connect-timeout 10 --max-time 30 "{base_url}/dy_fetch_user_post?sec_user_id={id}&max_cursor={cursor}&count=20&apikey={apikey}"
```

路由：`dy_fetch_user_post`（dy_ 前缀）

**分页：** 首页 `max_cursor=0`。每页保存最后一条的 `dy_max_cursor` → 下页传它。返回空时判定最后一页。

**API 03 字段（27个）：** 作者头像、作者昵称、作者ID、签名、作品简介、作品ID、账号ID、分享标题、分享链接、点赞数、评论数、转发数、下载数、推荐数、分享数、作品标题、作品封面、视频链接、音频链接、用户搜索、视频话题、创建时间、作者UID、收藏数、播放数、dy_max_cursor、min_cursor

### API 01（内部补作者数据）

```bash
curl -s --connect-timeout 10 --max-time 30 "{base_url}/dy_fetch_one_video_v3?aweme_id={列表首条ID}&apikey={apikey}"
```

取 `作者粉丝数`、`作者作品数`、`总赞数`、`视频时长`（API 03 无这些字段）。

**API 01 字段（28个）：** 比 API 03 多视频时长、作者粉丝数、作者作品数、总赞数、获取时间。无 dy_max_cursor、min_cursor、推荐数、用户搜索、作者UID。

**字段名差异：** API 01 `视频音频` ↔ API 03 `音频链接`

**API 01 失败兜底：** 列表正常展示，作者区显示 `（作者数据暂未获取）`。

**作者无作品：** API 03 返回空 → `「这个作者没有公开发布的作品」`

---

## Step 3 · 展示列表

```
📱 小熙宅剧 · 455个作品 · 225万赞
   签名：谢谢您点开我的主页 · 您的关注是我创作最大的动力

 1. [7620780] 邗沟覆船武则天大怒！狄仁杰再次... 👍5.2万
 2. [7625657] 元芳封神之战！独战铁手团11高手... 👍4.2万
...
20. [7657518] 狄仁杰一眼识破鬼怪骗局... 👍1176

📄 第1页  💡 下一页 | 查第3个看详情 | 导出
```

- 统一每页展示本页全部记录
- 序号动态计算：`已拉取总数 + 1`（不硬编码）
- 签名合并一行：`replace('\n', ' · ')`
- 时长 < 60s 显示秒，≥ 60s 显示 X分Y秒（查详情时有，列表无）
- 点赞 ≥ 10000 显示 `X.X万`
- API 01 失败时：`📱 小熙宅剧（作者数据暂未获取）`

---

## Step 4 · 翻页

| 用户说 | 处理 |
|--------|------|
| 「下一页」/「next」/「more」 | 取上页 `dy_max_cursor` → API 03。失败：`已拿到N个，导出这些/重试？` |
| 「上一页」/「back」/「prev」 | 从缓存取（最近 3 页，不调 API） |
| 「回到第1页」/「first」 | 从缓存取第1页 |
| 第一页说「上一页」 | 「这已经是第一页了～」 |
| 最后一页说「下一页」 | `🎉 全部看完！共N个 ✨ 💡 导出 | 回到第1页` |
| 第3页起 | 追加 `💡 导出全部 | 回到第1页` |
| **切作者** | 清空缓存 |

---

## Step 5 · 查某视频详情

用户说「查第3个」「第3个」「3」「看看第3个」「第3个详情」→ 用缓存中该视频的 `作品ID` 调 API 01 → 展示 API 01 完整 28 字段（三组表格：作者信息/作品数据/媒体链接 + 折叠 JSON），含视频时长、音频链接、播放数等列表不展示的字段。

**跨页：** 「查第25个」→ 自动算所在页 → 翻过去 → 展示。

**越界：** 「查第999个」→ 「只有 N 个视频，输入 1-N」

```
✅ 视频详情已展示
💡 ↩️ 返回列表 | 导出
```
返回列表时记住所在页码。

---

## Step 6 · 导出 Excel

读 `references/export-excel.md`。

用 API 03 数据导出，不逐条调 API 01（省积分省时间）。

**表头（17 列）：** 序号 | 作品ID | 作品简介 | 作品标题 | 点赞数 | 评论数 | 分享数 | 收藏数 | 播放数 | 转发数 | 下载数 | 推荐数 | 视频话题 | 创建时间 | 分享链接 | 视频链接 | 音频链接 | 作品封面

```
≤ 20 个 → 直接导出
> 20 个 → 「共N个，预计X分钟。全部导出 / 只导出当前页？」
```

- 逐页拉取，每页即写入（断点续传）
- 进度：`📥 正在导出... 第3页`
- 失败：「继续导出」从断点继续
- 完成：`✅ D:\xxx\dy_user_videos_{作者名}_{日期}.xlsx 双击打开～`
- 提示：`💡 需要时长？说「查第N个」看完整详情`

**「导出到飞书」：** 检查 `export.feishu.app_id`：
- 为空 → 「飞书还没配置。跟着教程走，5分钟搞定 👇」→ 读 `references/export-feishu-setup.md` → 逐步引导
- 已配置 → 获取 token → bitable_id 为空则自动创建多维表格 → 批量写入 → 删除飞书默认的 4 个空字段 → `✅ 已导入 N 条`
